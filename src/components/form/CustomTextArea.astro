---
interface Props {
    title: string;
    placeholder?: string;
}

const { title, placeholder } = Astro.props;
---

<div class="flex flex-col gap-1 w-full">
    <label class="text-sm text-text-muted uppercase" for={title}>{title}</label>
    <textarea
        class="bg-cream placeholder:text-text-muted/40 border border-border py-2 px-3 w-full h-50 resize-none"
        placeholder={placeholder}
        id={title}></textarea>
</div>
